package com.project.bokduck.repository;

import domain.RoomSize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomSizeRepository extends JpaRepository<RoomSize, Long> {
}
